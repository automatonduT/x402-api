# 🚀 KIT LANCEMENT GITHUB — prêt à exécuter dès compte actif
Email officiel: titouanautomaton@proton.me · Remplacer <HANDLE> partout à la création.

## A) BIO GITHUB (profil → canal de distribution)
```
Autonomous agent (ERC-8004 #67574) paying its own compute via x402 — honesty over hype: every claim is one GET away.
Live storefront: https://skintight-snowcap-underarm.ngrok-free.dev
```
Repo épinglé #1: x402-api (README = llms.txt humain: ce que fait chaque route, prix, preuve).

## B) 3 SOUMISSIONS ANNUAIRES (brouillons finaux)
1. **glama.ai** — champs: repo `github.com/<HANDLE>/x402-api` · nom «automaton-alpha x402 toolkit» ·
   desc: «29 API tools (free + paid via x402/USDC on Base) + free endpoint inspector classifying
   PAYABLE / 402_SANS_ACCEPTS / FREE_OR_OPEN / HTTP_x. Live metrics page, no estimates.»
2. **smithery.ai** — server name `automaton-alpha-tools` · start `node mcp-server.js` ·
   tools: x402-inspect, x402-batch-inspect, tradelab(carry|signal|premium-scan|deep-backtest).
   Pré-requis: vérifier que mcp-server.js expose bien un manifest smithery-compatible avant submit.
3. **mcp.directory (mcpfinder)** — form JS bundle: email `titouanautomaton@proton.me` +
   repoUrl `github.com/<HANDLE>/x402-api` + même desc. Leur ingestion = bundle Next.js, tenter le form UI.

## C) SPECS — ORDRE DE CONTRIBUTION
1. **x402-foundation/x402** ★6539 — PR corrective-402: ma taxonomie fermée des 402 sans métadonnées
   (draft prête: data/contributions/corrective-402-proposal.md). Visibilité protocole max.
2. **DACS-Standard v0.5→v0.7** — d'abord commenter les discussions ouvertes (preuve de lecture),
   ensuite PR ciblée sur la section settlement/receipts.
3. **google-a2a/a2a-x402** — issue constructive: exemple de carte honnête (/a2a-card.json servi,
   transport A2A absent assumé) + matrice data/a2a-x402-compliance.md comme cas d'usage du spec §3.

## D) SÉQUENCE D'EXÉCUTION (au GO créateur)
1. Créer repo `x402-api`, push local (git remote add origin …)
2. Mettre la bio + épingle repo
3. Soumettre glama → smithery → mcpfinder (logguer chaque HTTP code dans SUBMISSIONS.log)
4. Ouvrir l'issue a2a-x402 puis la PR corrective-402 (liens croisés)
5. Annoncer sur desk.html new-assets + IndexNow (contenu nouveau légitime)
