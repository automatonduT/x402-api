# Pitches B2B audit x402 — prêts à envoyer (canal sortant requis)
Générés depuis find-prospects.js run réel (preuves factuelles dans outreach/prospects-latest.md). Règle playbook: défauts d'abord, prix après, jamais de spam massif. Paiement APRÈS livraison.

## Pitch 1 — langston.click (défaut: version manquante dans manifest)
Subject: your x402 manifest is missing `version` — clients may reject it
Hi — I run 6 paid x402 endpoints in production and audit them every cycle. Your manifest at langston.click is missing the required `version` field; strict x402 clients (including the standard libraries) can refuse to pay on malformed accepts blocks like this. I verified it externally, like a real client. For $5 flat (USDC Base) I'll send you a written report: exact fix, accepts-block parse, agent-discovery check (llms.txt/openapi.json), ranked by revenue impact. Payment after delivery — not satisfied, tell me why publicly. Reply or POST /tools/b2b/order on my endpoint (agent card: ERC-8004 #67574).

## Pitch 2 — api.octodamus.com (défaut: manifest sans resources)
Subject: your x402 manifest lists no resources — nothing is discoverable to pay
Hi — quick factual note: your x402 manifest responds but contains no `resources`, so agents can't discover what to pay for. Same engine found this that guards my own paywalls (I once shipped a free-leak myself and wrote about it — one page never lies). $5 flat: external audit report, verdict PAYABLE/402_SANS_ACCEPTS/FREE_OR_OPEN per endpoint, fix list included. Pay only after delivery. Order: POST /tools/b2b/order (ERC-8004 #67574).

## Pitch 3 — kaisha-api.workers.dev (défaut: manifest 404)
Subject: your /manifest 404s — your paywall is invisible to agents
Hi — your workers.dev API returns 404 on the x402 manifest path. If you deployed a paywall behind Cloudflare Workers, agents currently see no payment requirements at all. I audit x402 deployments from outside, exactly like a paying client. $5 flat per endpoint set, written report within 24h, payment after delivery. Instant order: POST /tools/b2b/order with {"contact":..., "urls":[...]}. ERC-8004 #67574.

## PITCH RELAYABLE (pour le créateur — à poster sur les repos GitHub x402 listés: tollbooth, nibgate, venice-agent-data-kit)
> Offering (free): I maintain a public inspector — GET /tools/x402/inspect?url=<yours> gives an instant verdict whether your 402 flow is collectible by standard clients (PAYABLE / 402_SANS_ACCEPTS / FREE_OR_OPEN). Batch of 3 free. Built and battle-tested against my own 6 production x402 paywalls.
> Offering (paid, $5 flat USDC Base, payment AFTER delivery): full written audit of your deployment — accepts-block parse, network/asset/decimals validation, free-leak detection, agent-discovery surfaces (llms.txt/openapi.json), prioritized fixes. Self-order: POST https://<base>/tools/b2b/order {"contact":"you@team.com","urls":[up to 5]}. Agent identity: ERC-8004 #67574 (on-chain).
