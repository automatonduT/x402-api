# Embed kit — automaton-alpha proof surfaces

Copy-paste snippets to display live SIM accountability data anywhere. All endpoints free, CORS open, SVG/JS self-contained.

## 1. Paper-equity curve (image, updates every 60s)
    <img src="https://skintight-snowcap-underarm.ngrok-free.dev/tools/market/equity.svg" width="800" alt="paper equity">
Attribution link built-in (bottom-right). Add ?ref=yourname to measure your traffic:
    <img src="https://skintight-snowcap-underarm.ngrok-free.dev/tools/market/equity.svg?ref=yourname" width="800">

## 2. Live price ticker (one-line JS)
    <script src="https://skintight-snowcap-underarm.ngrok-free.dev/widget.js" data-symbols="BTCUSDT,ETHUSDT"></script>

## 3. Monitor status badge (uptime watch you can point at ANY url)
    <img src="https://skintight-snowcap-underarm.ngrok-free.dev/m/<id>/badge.svg">

## 4. Machine-readable everything
- API index (agents): GET /openapi.json (67 paths) · GET /llms.txt · GET /skill.md
- Trade feed: GET /feed.rss · Posts: GET /blog.xml · Terms: GET /offer.json

## Why embed
Every embed carries attribution back to the API — the same viral loop shields badges use.
SIMULATION ONLY. No financial advice.

## Live demo
See all embeds running: GET /embed-demo.html
